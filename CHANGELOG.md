# 更新日志

## 1.7.0 — 2026-09-20

### 修复与优化

- 定时执行后自动回读默认设备与应用设备规则，区分等待会话、核对成功和需处理的问题；支持立即执行／重试。核对结果不代表已测试真实声音。
- 完善当前电脑的备份恢复：包含音频方案、音频预设、设备别名、定时计划和补执行开关，兼容旧版备份；导入前检查格式、重复标识、引用关系及计划时间，显示新增／覆盖数量并确认后导入。未连接设备原样保留，导入不立即切换音频，也不补执行旧任务；暂不支持跨电脑设备匹配。
- 音频方案增加独立的选中状态，区分当前选中项与已生效方案；输出、输入信息分行显示，调整应用音频控件间距。
- 工具菜单新增直接打开系统音量合成器的入口。
- 发布流程增加回归检查、界面渲染和版本号校验，程序及安装包版本统一为 1.7.0。
- 统一界面命名：「配置」改为「音频方案」、「应用配置」改为「音频预设」、「应用覆盖」改为「应用设备规则」，同步菜单、定时计划、提示和导入导出文案；数据格式不变。
- 统一 12 个窗口的字体、按钮、输入框、下拉框、边框、列表选中状态与键盘焦点样式；弹窗统一「取消／保存」顺序，修正固定高度、长设备名及更新窗口按钮拥挤的问题。
- 补充标签颜色的键盘操作，修复细滚动条悬停颜色不生效，统一提示文字与禁用状态的视觉层级。
- 定时计划改为独立的新建／编辑弹窗，点击「添加计划」后才保存到列表，取消不产生计划。
- 修复重复计划尚未首次到点却补执行昨天／上周任务的问题：记录计划生效时间，新建、修改规则和重新启用均从之后的时间点开始；旧计划首次升级从当时起算，避免误补历史任务。补执行仅在启动或唤醒时触发。
- 暂时移除迷你窗口及主菜单、托盘和启动恢复入口，待重新设计后再添加。
- 切换配置时先清除所有应用已保存的输入／输出设备路由，再应用当前配置的覆盖规则；未运行的应用下次启动也会跟随系统默认设备，避免沿用上个配置。重新应用相同配置也会执行清理。
- 新增「工具 → 定时切换配置」，支持每天、指定星期、单次计划、启停和立即试运行，显示下次执行及最近结果；锁定时跳过，默认错过不补执行，同一时间按列表顺序处理冲突。
- 定时切换可选开启启动／唤醒后补执行，仅应用最近应生效的计划；已处理的时间点不重复，执行记录注明原定时间与补执行结果。
- 主窗口的配置改为右键菜单「切换到此配置／重新应用此配置」，左键点击配置名称、详情或卡片空白不再切换，减少误触。
- 应用覆盖来源列表不再按 EXE 合并多开实例，并显示进程编号以便区分；明确说明同程序实例可能共用 Windows 路由设置。
- 应用音频的静音和音量仅控制播放会话，不再影响录音输入；仅有录音会话的应用禁用输出音量控件。
- 应用音频会话改由 Windows 创建、状态变化和断开通知驱动，覆盖播放和录音设备；新会话出现后应用音频列表自动刷新。
- 应用路由区分「等待音频会话」「等待确认」「无法读取路由」和已确认偏离；查询失败不再被当作「跟随系统」。任一方向会话出现后即可设置并验证输入和输出，无需等待录音会话。
- 切换配置后暂缓验证，待会话出现时补设路由并有限重试；已确认成功的未锁定路由不会因后续检查被自动覆盖。
- 锁定后的修复必须读取验证成功才解除偏离；保留已知会话路由的低频核对，以覆盖未触发会话通知的外部修改。


## 1.6.1 — 2026-08-16

### 优化

- **菜单图标**：主窗口顶部菜单、托盘右键菜单、设备右键菜单的各项都补上了对应含义的图标（Segoe MDL2 图标字体），不再是纯文字

## 1.6.0 — 2026-08-16

### 新增

- **自动更新**：从组织仓库的 GitHub Releases 检测新版本
  - 手动检查：主窗口「帮助 → 检查更新…」、托盘菜单、设置窗口「检查更新」按钮，三处入口行为一致，始终有反馈（有更新 / 已是最新 / 检查失败）
  - 自动检查：设置窗口新增「自动检查更新」开关；开启时每次启动最多检查一次，并节流为每 24 小时最多一次；完全静默，只有发现未跳过的新版本才弹窗
  - 发现新版本时可「立即更新」（下载安装包 + 显示进度 + 以管理员权限拉起安装程序，安装完成后自动关闭并重启本程序）、「跳过此版本」（该版本不再自动提示，手动检查不受影响）、「以后再说」或「在浏览器中查看」发布页
  - 设置窗口显示当前版本号与上次检查时间

### 优化

- **应用配置列表**：每项的「输出」「输入」设备名称改为分两行显示，避免同一行挤在一起

## 1.5.0 — 2026-08-16

### 新增

- **应用音频：识别多开实例**：BlueStacks 等通过 `--instance` 启动参数区分的多开实例，现在正确采集到该信息；应用音频列表直接显示实例名（如「Antonio」），不再带应用名前缀（如「BlueStacks — Antonio」）
- 应用图标：为无法通过资源直接提取图标的程序（如 UWP / 受保护 exe）新增基于 Shell（`SHGetFileInfo`）的回退提取，与资源管理器保持一致

### 优化

- **应用音频窗口大幅提速**：
  - 打开窗口时的会话 / 设备 / 图标 / 进程信息采集迁移到后台线程，窗口一打开就能绘制并响应，不再整体卡顿发白
  - 进程信息 WMI 查询、按可执行文件路径匹配运行进程两处改为一次性批量查询，不再逐会话 / 逐行重复查询
  - 应用音量 / 静音初始状态改为在会话枚举时一并读取，省掉每行额外的一次全量音频设备/会话枚举
  - 播放 / 录音设备列表的重复枚举去重
- **应用音量滑块拖动更跟手**：改用节流（立即生效 + 限频 + 收尾兜底），替代原来「每次拖动都重新枚举系统进程 + 音频会话」的做法

## 1.4.0 — 2026-05-30

### 新增

- **导入 / 导出配置**：文件菜单新增「导出配置…」「导入配置…」，把设备配置 + 应用预设 + 设备别名打包成单个 JSON 备份文件，可在另一台电脑还原
  - 导入为**合并**模式：相同 ID 的配置 / 预设覆盖更新，新的追加；设备别名按设备合并。导入后自动重注册快捷键并刷新界面
  - 配置锁定时禁止导入（先解锁）

### 优化

- **配置持久化加固**：profiles / settings / app-profiles 三个文件改为崩溃安全写入
  - **原子写**：先写临时文件再替换，旧文件保留为 `.bak`；写入途中崩溃 / 断电只会留下旧文件或新文件，不再出现半截损坏文件
  - **自动恢复**：文件损坏时自动从 `.bak` 恢复；无法恢复时把损坏文件改名为 `.corrupt-<时间戳>` 保留，不再静默用默认值覆盖（修复了 settings 损坏会导致别名 / 隐藏 / 锁定等设置永久丢失的隐患）

## 1.3.0 — 2026-05-30

### 新增

- **测试播放**：播放设备右键菜单新增「测试播放」，在该设备上播放 1 秒测试音（直接指定设备输出，不依赖系统默认设备，可试听任意设备）
- **设备音量调节**：主窗口每个播放设备行新增音量滑块 + 静音按钮（🔊 / 🔇），调节该设备端点主音量与静音
  - 通过 `AudioEndpointVolume.OnVolumeNotification` 事件驱动，系统音量条 / 键盘音量键 / 其他程序的改动都会**实时**反映到对应设备行（零轮询开销）
- **应用音量调节**：应用音频窗口每个应用新增音量滑块 + 静音（per-app `SimpleAudioVolume`），可单独调节某个程序的音量；写入应用到该程序的所有进程

## 1.2.1 — 2026-05-23

### 修复

- **「关于」「设置」窗口内容被裁切**：窗口改为按内容自适应高度（`SizeToContent`），保存 / 取消等底部按钮和区块不再被裁掉

## 1.2.0 — 2026-05-23

### 新增

- **Voicemeeter 设备显示完整型号名**：设备选择下拉菜单和当前设备行显示 Windows 完整设备名（含蓝牙型号，如 `Headphones (HD 450BT)` / `(BX17)`），而非 Remote API 给出的泛化名 `Headphones [A]`；通过硬件 ID + 名字关联 + 枚举顺序配对，自动区分同名 / 同型设备
- **显示 Voicemeeter 虚拟输入**：Strip 列表加入虚拟输入（Voicemeeter VAIO / AUX / VAIO3），显示名称 + 静音开关（无硬件设备可选，故无设备下拉 / 电平条）

### 修复

- **设备名匹配排除 `[A]` / `[B]` 消歧后缀**：修复锁定还原、外部漂移检测、下拉勾选会把蓝牙设备错配成同名其他设备的问题（曾导致锁定还原到错误设备、反复抖动、勾选错位）
- **「已丢失」红标放宽**：已配对但暂时断开的设备（如蓝牙）不再误标红，仅在设备从系统彻底移除时才标红

### 优化

- 系统端点枚举与设备名映射表缓存（锁定轮询时不再每次完整枚举，降低开销）
- `voicemeeter.log` 超 512KB 自动轮转（保留一份 `.old`）+ 写入串行化；设备下拉日志精简为仅记录被补全的项

## 1.1.0 — 2026-04-26

### 新增

- **默认设备 peak 电平条**：主窗口默认播放 / 录音设备显示横向实时电平（NAudio `AudioMeterInformation`）；录音设备用 `WasapiCapture` keepalive，不再依赖系统录音界面打开
- **Voicemeeter 集成增强**：
  - Strip / Bus 实时 peak 电平条（`VBVMR_GetLevel`，零额外开销）
  - Strip 静音状态显示 + 一键切换（点击徽章），写入即时反馈
  - 设备丢失检测：Voicemeeter 配置的物理设备从系统消失时显示红色"已丢失"标签
  - Strip 静音状态锁定：外部从 Voicemeeter UI 修改会自动恢复，后台线程 50ms 紧轮询
- **配置锁定**：
  - 每个 profile 独立 🔒 / 🔓 切换按钮
  - 锁定后系统面板切换设备时立即恢复（COM 事件直驱，无 debounce）
  - 锁定时软件内点其他 profile / 设备右键"设为默认 / 启用 / 禁用"全部禁用 + 提示
- **Voicemeeter 集成开关**：设置里加"启用 Voicemeeter 集成"复选框（默认关闭）；关闭时不加载 DLL、不轮询，无开销
- **配置拖拽排序**：profile 卡片左侧 ☰ 句柄拖动调整顺序
- **配置标签颜色**：8 色预设（蓝/绿/红/黄/紫/粉/灰/无），主窗口和迷你窗口都显示色点
- **配置"在迷你窗口中显示"开关**

### 优化

- **主窗口布局**：播放 / 录音设备改为左右两栏，Voicemeeter 面板与配置区并排，整体紧凑
- **静音切换响应**：乐观更新 + pending writes 表，连续点击不会被异步刷新覆盖
- **后台轮询**：Voicemeeter 改为后台线程，锁定时 50ms 紧轮询，未锁定 1.5s

### 修复

- 主窗口最小化时正确停止 peak 计量，麦克风占用图标消失
- Voicemeeter 标题与"配置"标题视觉对齐

## 1.0.1 — 2026-04-25

### 优化

- 降低托盘进程内存占用（图标缓存 + COM 释放 + 去除冗余轮询）

## 1.0.0 — 2026-04-23

### 新增

- 首次发布
- 默认设备一键切换（未文档化的 `IPolicyConfig` COM API）
- 配置管理：保存常用设备组合（输出 + 输入），带全局快捷键绑定
- 应用级音频路由：按应用覆盖默认音频设备（`AppAudioRoutingService` + 应用音频窗口）
- 应用配置（独立预设）：可复用的应用音频预设，挂到配置上（`AppProfile` + `DeviceProfile.AppOverrides`）
- 托盘常驻 + 迷你窗口（无边框、置顶、可调透明度）
- 蓝牙检测（识别 BTHENUM 设备）
- 设备管理：重命名（别名）、隐藏、启用 / 禁用
- Toast 通知、开机自启、记住窗口位置与设置、自定义应用图标
