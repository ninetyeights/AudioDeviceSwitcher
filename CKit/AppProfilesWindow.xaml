<Window Style="{StaticResource AppWindow}" x:Class="AudioDeviceSwitcher.AppProfilesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="音频预设" Height="460" Width="560" MinWidth="480" MinHeight="320"
        Icon="pack://application:,,,/Resources/app.ico"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" FontSize="13" Foreground="#4B5563" TextWrapping="Wrap" Margin="0,0,0,8">
            保存输入与输出设备组合，供音频方案中的应用设备规则使用。
        </TextBlock>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
            <Button Content="新建" Padding="12,4" Click="New_Click"/>
            <TextBlock x:Name="EmptyHint" Text="还没有音频预设，点 新建 开始"
                       VerticalAlignment="Center" Margin="12,0,0,0" FontSize="12" Foreground="#6B7280"
                       Visibility="Collapsed"/>
        </StackPanel>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="ProfileList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#F9FAFB" BorderBrush="#E5E7EB" BorderThickness="1"
                                CornerRadius="6" Padding="12,8" Margin="0,0,0,6">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="{Binding Name}" FontSize="14" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" ToolTip="{Binding Name}"/>
                                    <TextBlock FontSize="11" Foreground="#6B7280" TextTrimming="CharacterEllipsis" ToolTip="{Binding OutputDeviceName}">
                                        <Run Text="输出: "/>
                                        <Run Text="{Binding OutputDeviceName, Mode=OneWay, FallbackValue=跟随系统, TargetNullValue=跟随系统}"/>
                                    </TextBlock>
                                    <TextBlock FontSize="11" Foreground="#6B7280" TextTrimming="CharacterEllipsis" ToolTip="{Binding InputDeviceName}">
                                        <Run Text="输入: "/>
                                        <Run Text="{Binding InputDeviceName, Mode=OneWay, FallbackValue=跟随系统, TargetNullValue=跟随系统}"/>
                                    </TextBlock>
                                </StackPanel>
                                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                    <Button Content="编辑" Padding="8,2" Margin="0,0,6,0"
                                            Tag="{Binding Id}" Click="Edit_Click"/>
                                    <Button Content="删除" Padding="8,2" Foreground="#D32F2F"
                                            Tag="{Binding Id}" Click="Delete_Click"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <Button Grid.Row="3" Content="关闭" Padding="16,6" HorizontalAlignment="Right"
                Margin="0,8,0,0" Click="Close_Click"/>
    </Grid>
</Window>
